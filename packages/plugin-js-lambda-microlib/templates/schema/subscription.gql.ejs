<%

const queryNames = Object.keys(schema.subscriptions);
queryNames.sort();

-%>
type Subscription {
<% queryNames.forEach(name => { const item = schema.subscriptions[name]; %>    <%= name %>(): <%= item.gqlType || 'String' %>
<% }) -%>
}
