module.exports = r => <%- ('undefined' !== typeof resolverBuilder) ? `require('${resolverBuilder}')` : '' -%>({
<% Object.entries((('undefined' !== typeof resolvers) ? resolvers : undefined) || {}).forEach(([typeName, queries]) => { -%>
    <%- typeName -%>: {
<% Object.entries(queries).forEach(([k, v]) => { -%>
        <%- k -%>: r('<%- v -%>'),
<% }) -%>
    },
<% }) -%>
});