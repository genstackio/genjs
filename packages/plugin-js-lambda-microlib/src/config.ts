const config = {
    handlers: {
        graphql: {
            files: {
                'resolvers.js': true,
                'app.js': true,
                'schema.files': true,
            }
        }
    }
};

export default config;