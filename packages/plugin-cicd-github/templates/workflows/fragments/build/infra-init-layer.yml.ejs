      - name: Init (Infrastructure) of layer <%= layer.name %>
        shell: 'script -q -e -c "bash {0}"'
        run: make infra-init layer=<%= layer.name %> env=${{env.env}}
        env:
          FORCE_COLOR: 3
          CI: true