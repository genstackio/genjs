      - name: Provision layer <%= layer.name %>
        shell: 'script -q -e -c "bash {0}"'
        run: make provision layer=<%= layer.name %> env=${{env.env}}
        env:
          FORCE_COLOR: 3
          CI: true