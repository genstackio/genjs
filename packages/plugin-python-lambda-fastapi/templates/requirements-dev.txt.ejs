<% Object.entries({...('undefined' === typeof dependencies ? {} : dependencies), ...('undefined' === typeof devDependencies ? {} : devDependencies)}).forEach(([name, constraint]) => { -%>
<%= name %><%- constraint %>
<% }) -%>