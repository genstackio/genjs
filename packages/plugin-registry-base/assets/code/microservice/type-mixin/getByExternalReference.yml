functions:
  getBy: getBy(type=<%- model.shortName %>)

operations:
  getByExternalReference:
    type: getBy(externalReference)
    args:
      - {name: externalReference, type: string, outputType: String, required: true}
