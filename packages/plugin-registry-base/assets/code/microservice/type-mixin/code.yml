inputs:
  optional:
    type: boolean
    default: false
  indexed:
    type: boolean
    default: true
  unique:
    type: boolean
    default: false

attributes:
  code: "<%- unique ? '!' : '' %><%- indexed ? '@' : '' %>string<%- optional ? '' : '!' %>"
