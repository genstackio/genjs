<% if (groups.length) { -%>
<% groups.forEach(group => { -%>
<% if ((group.name !== '@' && group.name !== 'General') || (groups.length > 1)) { %><%- group.enclose ? '###> ' : '# '%><%- group.name === '@' ? 'General' : group.name %><%- group.enclose ? ' ###' : ''%>
<% } -%>
<% group.ignores.forEach(ignore => { -%><%- ignore.path %>
<% }) -%>
<% if (group.enclose && ((group.name !== '@' && group.name !== 'General') || (groups.length > 1))) { %>###< <%- group.name === '@' ? 'General' : group.name %> ###
<% } %>
<% }) -%><% } -%>
